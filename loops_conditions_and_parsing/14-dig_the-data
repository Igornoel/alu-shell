#!/usr/bin/env bash
# Groups Apache log entries by IP and HTTP status code, sorted by occurrence count
awk '{print $1 " " $9}' apache-access.log | sort | uniq -c | sort -rn | awk '{printf "   %d %s %s\n", $1, $2, $3}'
